apiVersion: v1
kind: ServiceAccount
metadata:
  name: chat-svc
  namespace: team-chat
  annotations:
    # Injected by Flux substitution; the machine-generated identifier is not committed.
    azure.workload.identity/client-id: "${CHAT_SVC_CLIENT_ID}"
