apiVersion: v1
kind: ServiceAccount
metadata:
  name: chat-svc
  namespace: team-chat
  annotations:
    # Replace from `pulumi stack output chatSvcClientId`; see README.md.
    azure.workload.identity/client-id: "<CHAT_SVC_CLIENT_ID>"
