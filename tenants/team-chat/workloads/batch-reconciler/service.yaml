apiVersion: v1
kind: Service
metadata:
  name: batch-status
  namespace: team-chat
  labels:
    app.kubernetes.io/name: batch-status
spec:
  selector:
    app.kubernetes.io/name: batch-status
  ports:
    - name: http
      port: 8000
      targetPort: http
